<Window x:Class="wpf_project.ChangePass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_project"
        mc:Ignorable="d"
        Title="ChangePass" Height="250" Width="350" WindowStartupLocation="CenterScreen" MaxHeight="250" MinHeight="250" MaxWidth="350" MinWidth="350">
    <Grid>
        <StackPanel Orientation="Vertical">
            <GroupBox Header="Введите старый пароль" FontFamily="Roboto" FontSize="16" FontWeight="DemiBold" Margin="5">
                <PasswordBox Name="tbLastPass"></PasswordBox>
            </GroupBox>
            <GroupBox Header="Введите новый пароль" FontFamily="Roboto" FontSize="16" FontWeight="DemiBold" Margin="5">
                <PasswordBox Name="tbNewPass"></PasswordBox>
            </GroupBox>
            <GroupBox Header="Повторите новый пароль" FontFamily="Roboto" FontSize="16" FontWeight="DemiBold" Margin="5">
                <PasswordBox Name="tbNewPass2" MaxLength="11"></PasswordBox>
            </GroupBox>
            <Button Style="{DynamicResource AcceptButtonStyle}" Click="Button_Click" Height="25" Width="140" Padding="1" Margin="2">Изменить пароль</Button>
        </StackPanel>
    </Grid>
</Window>
